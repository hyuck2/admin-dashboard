# ============================================================
# Admin Dashboard - Environment Variables
# 이 파일을 .env 로 복사한 후 값을 수정하세요
# cp .env.example .env && vi .env
# ============================================================

# --- Docker Image Tags ---
BACKEND_TAG=v0.2.1
FRONTEND_TAG=v0.3.2

# --- MySQL ---
MYSQL_ROOT_PASSWORD=admin
MYSQL_PASSWORD=admin

# --- Dashboard 접속 포트 ---
# 브라우저에서 http://<서버IP>:<DASHBOARD_PORT> 로 접속
DASHBOARD_PORT=8080

# --- 인증 ---
JWT_SECRET=banana-admin-secret

# --- SSH 비밀번호 암호화 키 (선택) ---
# 서버 등록 시 입력한 SSH 비밀번호를 DB에 암호화 저장하는 키
# 미설정 시 JWT_SECRET 에서 자동 파생
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# --- Git 연동 (앱 배포 기능 사용 시, 선택) ---
# GitHub/GitLab Personal Access Token
GIT_TOKEN=
# Deploy 저장소 URL (helm charts + app values)
DEPLOY_GIT_URL=https://github.com/your-org/banana-deploy.git
